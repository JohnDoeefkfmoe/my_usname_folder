name: test yml in nvim
on: [push]
jobs:
  mynameant:
   runs-on: ubuntu-latest
   steps:
    - name: first-step
      run: echo 'Hello world' > hw-file.txt
    - name: edit file hw
      run echo 'Hello world 2' > hw-file.txt
